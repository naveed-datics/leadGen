/** Standard provider target for every city query (city, state, and bulk). */
export const PER_CITY_TARGET = 300;

/** Leave ~60s headroom under the search route's 300s maxDuration. */
export const BULK_TIMEBOX_MS = 240_000;
